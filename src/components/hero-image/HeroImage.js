import { Box } from "@mui/material";
import React from "react";

const HeroImage = () => {
  return (
    <Box
      minHeight="100vh"
      sx={{ backgroundColor: "red" }}
    >
      HeroImage
    </Box>
  );
};

export default HeroImage;
